{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Meeting edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('mdurys_gupek_meeting_index') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
